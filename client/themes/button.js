import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    width: 80,
    margin: 5,
    marginTop: 0,
    padding: 5,
  },
  buttonText: {
    textAlign: 'center',
  },
});

export default styles;
